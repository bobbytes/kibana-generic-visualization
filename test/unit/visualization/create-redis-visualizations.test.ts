import { createRedisVisualizations } from '../../../src/visualization/create-redis-visualization';

describe('createRedisVisualizations', () => {
  test('first test', () => {
    expect(createRedisVisualizations).toBeDefined();
  });
});
